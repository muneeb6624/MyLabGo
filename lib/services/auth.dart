class AuthenticationService {
  // Placeholder for authentication methods (login, registration)
  Future<void> registerUser(String email, String password) async {
    // Call to backend for registration
  }
}
